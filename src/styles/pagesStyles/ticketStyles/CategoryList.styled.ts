import styled from 'styled-components';

export const CatergoryList = styled.div`
  width: 100%;
  height: fit-content;
  background: var(--white);

  display: flex;
  justify-content: space-between;
  flex-wrap: wrap;

  margin-top:1.6rem;
`;